# Per-repo sync overrides — see docs/sync-overrides.md
#
# Documented, intentional divergences from the Mergepath canonical mirror.
version: 1

# No overrides. The `scripts/ci/` skip that lived here was removed once its
# stated removal condition was met: it existed only because the canonical
# `scripts/ci/check_coderabbit_config` rejected this repo's PAIRED CodeRabbit
# opt-out (`reviews.auto_review.enabled: false` in `.coderabbit.yml` together
# with `coderabbit.enabled: false` in `.github/review-policy.yml`), and the
# canonical gate now accepts that form directly — nathanjohnpayne/mergepath#911,
# fixed and closed 2026-08-18.
